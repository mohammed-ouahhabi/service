<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome to Our Store{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 128 128'><text y='1.2em' font-size='96'>🛍️</text></svg>">
    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
        <style>
            :root {
                --primary-color: #0077b6;
                --secondary-color: #90e0ef;
                --accent-color: #00b4d8;
                --danger-color: #dc3545;
                --font-primary: 'Poppins', sans-serif;
                --background-color: #e0f2fe;
            }

            body, html {
                height: 100%;
                margin: 0;
                padding: 0;
                font-family: var(--font-primary);
                background: var(--background-color);
                color: #333;
                font-size: 16px;
            }

            .navbar {
                padding: 1rem 2rem;
                background: linear-gradient(120deg, var(--secondary-color), var(--accent-color));
                backdrop-filter: blur(15px);
                border-bottom: 3px solid var(--primary-color);
            }

            .navbar-brand {
                font-weight: 700;
                font-size: 1.5em;
                text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
                transition: transform 0.5s ease;
            }

            .navbar-brand:hover {
                transform: scale(1.05);
                text-shadow: 2px 2px 3px rgba(0, 0, 0, 0.2);
            }

            .nav-link {
                position: relative;
                overflow: hidden;
                transition: color 0.3s ease;
            }

            .nav-link::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 2px;
                background-color: var(--primary-color);
                transform: translateX(-101%);
                transition: transform 0.3s ease;
            }

            .nav-link:hover::after, .nav-link:focus::after {
                transform: translateX(0%);
                background-color: #ffffff;
            }

            .nav-link:hover, .nav-link:focus {
                color: #ffffff;
            }

            /* Custom styles for buttons and add product form */
            .btn {
                margin: 10px; /* Adjust margin as needed */
            }

            .add-product-form {
                margin: 20px auto; /* Adjust margin as needed */
                max-width: 500px; /* Adjust max-width as needed */
                padding: 20px; /* Adjust padding as needed */
                background-color: #ffffff; /* Add a white background */
                border-radius: 10px; /* Add border radius */
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
            }

            .nav-link.active, .nav-link:active {
                background-color: var(--primary-color);
                box-shadow: inset 0 3px 5px rgba(0, 0, 0, 0.2); /* creates the inset shadow */
                border-radius: 15px; /* to match your image style */
                color: white;
            }

            .nav-link.active {
                background-color: var(--primary-color); /* Active link background color */
                color: #ffffff; /* Active link text color */
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Shadow for a subtle 3D effect */
            }

            /* This ensures that the effect is removed when you hover over other items */
            .nav-link:not(.active):hover, .nav-link:not(.active):focus {
                background-color: transparent;
                color: white;
            }

            .nav-link:not(.active):hover {
                color: #ffffff; /* Color change for hover state */
                background-color: rgba(255, 255, 255, 0.1); /* Light background on hover for non-active links */
            }

            @keyframes spin {
                from {
                    transform: rotate(0deg);
                }
                to {
                    transform: rotate(360deg);
                }
            }

            .navbar-sticky {
                position: fixed;
                top: 0;
                width: 100%;
                transform: translateY(-100%);
                animation: slideDown 0.5s forwards;
            }

            @keyframes slideDown {
                to {
                    transform: translateY(0);
                }
            }

            .nav-item.active .nav-link {
                position: relative;
                box-shadow: 0 0 15px var(--primary-color);
                border-radius: 10px;
            }

            .navbar-brand:hover {
                animation: spin 1s ease-in-out;
            }

            .navbar {
                padding: 1rem 2rem;
                background: linear-gradient(120deg, var(--secondary-color), var(--accent-color));
                backdrop-filter: blur(15px);
                border-bottom: 3px solid var(--primary-color);
            }

            .navbar-brand {
                font-weight: 700;
                font-size: 1.5em;
                text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
                transition: transform 0.5s ease;
            }

            .navbar-brand:hover {
                transform: scale(1.05);
                text-shadow: 2px 2px 3px rgba(0, 0, 0, 0.2);
            }

            .nav-link {
                position: relative;
                overflow: hidden;
                transition: color 0.3s ease;
            }

            .nav-link::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 2px;
                background-color: var(--primary-color);
                transform: translateX(-101%);
                transition: transform 0.3s ease;
            }

            .nav-link:hover::after, .nav-link:focus::after {
                transform: translateX(0%);
                background-color: #ffffff;
            }

            .nav-link:hover, .nav-link:focus {
                color: #ffffff;
            }


            /* Custom dropdown styles if you have dropdown menus */
            .dropdown-menu {
                background-color: var(--secondary-color);
                border: none;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            }

            .dropdown-item {
                transition: background-color 0.3s ease;
            }

            .dropdown-item:hover, .dropdown-item:focus {
                background-color: var(--accent-color);
                color: #ffffff;
            }

            /* Style for all nav-links */
            .nav-link {
                background-color: transparent;
                color: #ffffff; /* Assuming white text for non-active links */
                border-radius: 15px;
                transition: color 0.3s ease, background-color 0.3s ease;
                padding: 8px 15px; /* Adjust the padding to your liking */
            }

            /* Only for non-active links */
            .nav-link:not(.active) {
                background-color: transparent; /* Transparent background for non-active */
                box-shadow: none; /* No shadow for non-active links */
            }

            /* When non-active links are hovered */
            .nav-link:not(.active):hover {
                background-color: rgba(255, 255, 255, 0.2); /* Slight white background on hover */
                color: #ffffff; /* White text */
                transition: background-color 0.3s ease; /* Smooth transition for hover effect */
            }

            .navbar-nav .nav-link:hover {
                color: #ffffff; /* Change to desired hover color */
                background-color: #007bff; /* Change to desired hover background color */
            }

            .navbar-nav .nav-link {
                transition: color 0.3s ease, background-color 0.3s ease;
            }

            .nav-link:hover {
                color: #ffffff; /* Change text color on hover */
                background-color: var(--primary-color); /* Change background color on hover */
            }

            .user-name .nav-link {
                font-weight: bold; /* Makes the font bold */
                color: var(--secondary-color); /* Uses a lighter color defined in your root */
                background-color: var(--primary-color); /* Background color for emphasis */
                border-radius: 10px; /* Rounded borders for aesthetic appeal */
                padding: 5px 10px; /* Proper padding for better visibility */
                box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.2); /* Subtle shadow for a 3D effect */
                transition: all 0.3s ease; /* Smooth transition for hover effects */
            }

            .user-name .nav-link:hover {
                color: var(--accent-color); /* Changes text color on hover */
                background-color: var(--secondary-color); /* Lighter background on hover */
                transform: scale(1.05); /* Slightly increases size on hover */
            }


        </style>
    {% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">MonSite</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.attributes.get('_route') == 'app_home' ? 'active' : '' }}"
                           href="{{ path('app_home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.attributes.get('_route') == 'app_statistique' ? 'active' : '' }}"
                           href="{{ path('app_statistique') }}">Statistique</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.attributes.get('_route') == 'app_shoes' ? 'active' : '' }}"
                           href="#">Shoes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_user_index') }}">Users</a>
                    </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item user-name">
                        <a class="nav-link">{{ app.user.firstname }} {{ app.user.lastname }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_login') }}">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% block body %}{% endblock %}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}

</body>
</html>
